<% if @user.errors.any? %>
<div id="errors_explanation">
  <h2><%= pluralize(@user.errors.count, "errors")%>
    prohibited this account to be created:</h2>
</div>
<ul>
  <% @user.errors.full_messages.each do |msg| %>
  <li>
    <%= msg %>
  </li>
  <%end%>
</ul>
<%end%>
<fieldset>
  <legend>Sigh Up</legend>
</fieldset>
<%= simple_form_for(@user,:html => { :class => 'form-horizontal' }) do |f| %>
<%= f.input :first_name, label: "First Name" %>
<%= f.input :last_name, label: "Last Name" %>
<%= f.input :email, label: "Email" %>
<%= f.input :password, label: "Password"%>
<%= f.input :password_confirmation, label: "Password confirmation" %>
<%= f.input :business_name, label: "Business Name" %>
<%= f.input :phone_number, label: "Phone Number" %>
<%= f.input :cell_number, label: "Cell Number" %>
<%= f.input :tax_identification, label: "Tax ID" %>
<%= f.input :ssn, label: "SSN #" %>
<%= f.input :address,label:  "Street Address" %>
<%= f.input :city, label: "City" %>
<%= f.input :zip_code, label: "Zip Code" %>
<%= f.input :state, :label => "State", :collection => us_states, :selected => "New Jersey"%>
<%= f.input :account_type, :label => "Business Type", :collection => biz_types, :selected => "Retail"%>
<%= f.input :y_in_biz, label: "Years in Business"%>
<div class="form-actions">
  <%= f.submit "Sign up", :class => 'btn btn-primary' %>
</div>

<%end%>
